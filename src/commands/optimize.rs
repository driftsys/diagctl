use std::path::Path;

pub fn run(_file: &Path) -> anyhow::Result<i32> {
    eprintln!(
        "optimize: not yet implemented (tracking: https://github.com/driftsys/metapowers/issues/14)"
    );
    Ok(2)
}
